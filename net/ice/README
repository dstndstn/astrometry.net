This directory contains an Ice setup like this:

-starttunnel running on oven creates a tunnel to the Glacier2 router.
-python SessionServer.py running on cluster24 handles Glacier2 sessions.
-startglacier2 on cluster24.
-startreg on cluster24 -- registry
-startnode on clusterXX -- workers
-python SolverClient.py -- using the Glacier2 router, contacts the registry on cluster24 and uses it to reach servers on clusterXX.



